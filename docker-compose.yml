services:
  oracle:
    image: gvenzl/oracle-free:23-slim-faststart
    ports:
      - "1521:1521"
    environment:
      ORACLE_PASSWORD: oracle
      APP_USER: life_circle
      APP_USER_PASSWORD: life_circle
    volumes:
      - oracle-data:/opt/oracle/oradata

volumes:
  oracle-data:
